# Artifact template asset build evidence — 2026-08-29

## Scope and result

Built three fact-free native template candidates from scratch. No prior artifact bytes, world facts, identities, signatures, approvals, or submission files were used or modified.

| Template | Native asset | SHA-256 | Native structure |
| --- | --- | --- | --- |
| `TMPL-0001@1.0.0` | `library/templates/TMPL-0001/1.0.0/internal-close-reconciliation.xlsx` | `fa0d1404c3bb5aecb9949238ffdfcb9fb5600a6fdb5fa345125117379d782762` | 8 sheets; formula-driven reconciliation, prior-period comparison, and checks; no external-link parts |
| `TMPL-0002@1.0.0` | `library/templates/TMPL-0002/1.0.0/internal-controller-memo.docx` | `af92ae888768c8327f0a1bb8e57d96c8f7fe2606708783812f20bcc9374f7759` | 5 rendered pages; 4 explicit page breaks; no comments or external-link parts; visibly marked not approved |
| `TMPL-0003@1.0.0` | `library/templates/TMPL-0003/1.0.0/operational-correction-thread.eml` | `ac002c90d10a19f7aab37b1cf38f3d47ff7042f82886b9739b47de157df47c49` | Valid `multipart/mixed`; one text body; CSV and plain-text attachments; deterministic MIME boundary |

Catalog entries remain `candidate`. They must not be consumer-discoverable as released until a separate same-hash release record and conductor decision bind the bytes.

## Toolchain gate

The repository document stack was activated before artifact authoring. Managed Node, managed Python, `@oai/artifact-tool`, LibreOffice, Poppler, Git, and Office executables passed. The all-profile result was `CANNOT_CHECK` only because optional `jsonschema` and Office COM imports were absent; generation and render capabilities were available and passed.

## Native verification

- Workbook: all 8 sheets were rendered and visually inspected. Post-export inspection found 60 reconciliation formulas, 20 prior-period formulas, 11 check formulas, zero formula-error matches, and an intentional `MODEL STATUS: FAIL` while required build tokens remain. Evidence: `xlsx-postexport-verify.ndjson` and `renders/xlsx/`.
- Memorandum: LibreOffice conversion produced 5 pages. Every page was visually inspected after a focused repair to shorten narrow-column tokens; no clipping, overlap, broken tables, or decorative title residue remained. Evidence: `renders/docx/`.
- Email: the Python standard-library parser opened the message and both attachments. Attachment content types are `text/csv` and `text/plain`; neither payload is empty.
- Hash drift: the asset test suite appends one byte to a temporary copy and proves that the recorded hash no longer matches.

## Test gates

`python -m unittest tests.test_template_assets -v`

- Result: PASS, 10 tests.
- Proves catalog discovery fields, safe path resolution, descriptor lineage, native hashes, declared build tokens, required capabilities, native XLSX/DOCX/EML structure, attachment parsing, mixed-manifest exact-version references, and byte-drift detection.

`python -m unittest discover -s tests -v`

- Result: PASS, 27 tests.

## Residual release work

This slice does not create or claim release records. A control-plane change must bind these exact hashes to blueprint hashes, sanitization evidence, independent Terra/Sol reviews, required proof records, and conductor approval before catalog status changes to `released`.
