# Artifact template asset build evidence — 2026-08-29

## Scope and result

Built three fact-free native template candidates from scratch. No prior artifact bytes, world facts, identities, signatures, approvals, or submission files were used or modified.

| Template | Native asset | SHA-256 | Native structure |
| --- | --- | --- | --- |
| `TMPL-0001@1.0.0` | `library/templates/TMPL-0001/1.0.0/internal-close-reconciliation.xlsx` | `26f97baff5b2ff007d18f3b47a11736f9362a91462ce256a7126a82f477a4fa5` | 8 sheets; bounded typed table capacities; guarded formula-driven reconciliation and readiness checks; explicit one-page print areas; no external-link parts |
| `TMPL-0002@1.0.0` | `library/templates/TMPL-0002/1.0.0/internal-controller-memo.docx` | `1a922e87a29682bdea8a04734084f9f9fe1631bf785139b95b2b0f74c238818f` | 5 rendered pages; 4 explicit page breaks; minimal empty core/extended properties; visibly marked not approved |
| `TMPL-0003@1.0.0` | `library/templates/TMPL-0003/1.0.0/operational-correction-thread.eml` | `8acaf754686fa23b1bbc59d229094d6444b54224ea9a0dae53c50086b8c5bf99` | Valid `multipart/mixed`; 6-message quoted chain; CSV and plain-text attachments; neutral deterministic MIME boundary; recipient-neutral final greeting |

Catalog entries remain `candidate`. They must not be consumer-discoverable as released until a separate same-hash release record and conductor decision bind the bytes.

## Toolchain gate

The repository document stack was activated before artifact authoring. Managed Node, managed Python, `@oai/artifact-tool`, LibreOffice, Poppler, Git, and Office executables passed. The all-profile result was `CANNOT_CHECK` only because optional `jsonschema` and Office COM imports were absent; generation and render capabilities were available and passed.

## Native verification

- Workbook: LibreOffice produced exactly 8 pages for 8 sheets after explicit print-area, fit-to-page, orientation, filter, and frozen-header controls were applied. All pages were visually inspected; no split or near-empty overflow pages remain. Empty reconciliation rows now remain blank, required inputs report `NOT READY`, optional empty entries report `NOT APPLICABLE`, and `MODEL STATUS` cannot pass without source rows, mapping rows, reconciliation rows, and resolved identity fields. Formula text contains no build tokens. The descriptor rejects rows beyond the six declared native-table capacities until formulas, tables, print setup, and render evidence are rebuilt together. Evidence: `xlsx-postexport-verify.ndjson`, `renders/xlsx/`, `renders/xlsx-print/`, and `render-manifest.json`.
- A fresh temporary minimal instantiation populated all required identities plus balanced source, mapping, and reconciliation rows. LibreOffice recalculation returned `PASS` for every applicable check and `MODEL STATUS`, `NOT APPLICABLE` for empty optional entries, and zero formula-token matches.
- Memorandum: LibreOffice conversion produced 5 pages. Every page was visually inspected after removing repository/build headers, footer language, instantiation instructions, and stale metadata; no clipping, overlap, broken tables, or decorative title residue remained. Evidence: `renders/docx/`.
- Email: the Python standard-library parser opened a 6-message quoted chain and both attachments with no parse defects. Attachment content types are `text/csv` and `text/plain`; neither payload is empty. Repository-specific headers and boundary text are absent.
- Leakage/residue: automated scans cover visible OOXML, core/extended metadata, MIME headers, decoded bodies, and attachments. They reject repository names, template/build instructions, stale dates, template addresses, and generator residue.
- Hash and render drift: the asset test suite detects native-byte changes and proves that an asset or descriptor hash change is rejected until the byte-bound render manifest is refreshed with the PDF and every page-image hash.

## Test gates

`python -m unittest tests.test_template_assets -v`

- Result: PASS, 18 tests.
- Proves catalog discovery fields, safe path resolution, exact descriptor/catalog bindings, blueprint identity without duplicated lineage, native and render-evidence hashes, declared field tokens, residue absence, bounded typed table capacities, over-capacity rejection, non-vacuous empty-state checks, required capabilities, native XLSX/DOCX/EML structure, print controls, recipient semantics, attachment parsing, six-message footprint, mixed-manifest exact-version references, and byte-drift detection. Release semantics remain owned by `scripts/validate_library.py` after control-plane integration.

`python -m unittest discover -s tests -v`

- Result: PASS, 35 tests.

## Residual release work

This slice does not create or claim release records. A control-plane change must bind these exact hashes to blueprint hashes, sanitization evidence, independent Terra/Sol reviews, required proof records, and conductor approval before catalog status changes to `released`.
