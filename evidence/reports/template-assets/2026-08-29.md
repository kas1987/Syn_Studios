# Artifact template asset build evidence — 2026-08-29

## Scope and result

Built three fact-free native template candidates from scratch. No prior artifact bytes, world facts, identities, signatures, approvals, or submission files were used or modified.

| Template | Native asset | SHA-256 | Native structure |
| --- | --- | --- | --- |
| `TMPL-0001@1.0.0` | `library/templates/TMPL-0001/1.0.0/internal-close-reconciliation.xlsx` | `fdd95ccdf5cd3c8b694a2caa4778768e1fd1d3303ee23d141f7e64a192b42b71` | 8 sheets; formula-driven reconciliation, prior-period comparison, and checks; explicit one-page print areas; no external-link parts |
| `TMPL-0002@1.0.0` | `library/templates/TMPL-0002/1.0.0/internal-controller-memo.docx` | `8372dc733ba8a49e0a686b13157e80b3fc59ef8b35dc3e64f35bfaf1efc221d5` | 5 rendered pages; 4 explicit page breaks; minimal empty core/extended properties; visibly marked not approved |
| `TMPL-0003@1.0.0` | `library/templates/TMPL-0003/1.0.0/operational-correction-thread.eml` | `bff820ee2221da30a61b952fa41350a111e4da208359012b0e70e81b5346a63d` | Valid `multipart/mixed`; 6-message quoted chain; CSV and plain-text attachments; neutral deterministic MIME boundary |

Catalog entries remain `candidate`. They must not be consumer-discoverable as released until a separate same-hash release record and conductor decision bind the bytes.

## Toolchain gate

The repository document stack was activated before artifact authoring. Managed Node, managed Python, `@oai/artifact-tool`, LibreOffice, Poppler, Git, and Office executables passed. The all-profile result was `CANNOT_CHECK` only because optional `jsonschema` and Office COM imports were absent; generation and render capabilities were available and passed.

## Native verification

- Workbook: LibreOffice produced exactly 8 pages for 8 sheets after explicit print-area, fit-to-page, orientation, filter, and frozen-header controls were applied. All pages were visually inspected; no split or near-empty overflow pages remain. Post-export inspection found 60 reconciliation formulas, 20 prior-period formulas, 11 check formulas, zero formula-error matches, and an intentional `MODEL STATUS: FAIL` while required close fields remain unresolved. Evidence: `xlsx-postexport-verify.ndjson`, `renders/xlsx/`, and `renders/xlsx-print/`.
- Memorandum: LibreOffice conversion produced 5 pages. Every page was visually inspected after removing repository/build headers, footer language, instantiation instructions, and stale metadata; no clipping, overlap, broken tables, or decorative title residue remained. Evidence: `renders/docx/`.
- Email: the Python standard-library parser opened a 6-message quoted chain and both attachments with no parse defects. Attachment content types are `text/csv` and `text/plain`; neither payload is empty. Repository-specific headers and boundary text are absent.
- Leakage/residue: automated scans cover visible OOXML, core/extended metadata, MIME headers, decoded bodies, and attachments. They reject repository names, template/build instructions, stale dates, template addresses, and generator residue.
- Hash drift: the asset test suite appends one byte to a temporary copy and proves that the recorded hash no longer matches.

## Test gates

`python -m unittest tests.test_template_assets -v`

- Result: PASS, 14 tests.
- Proves catalog discovery fields, safe path resolution, exact descriptor/catalog bindings, repaired blueprint lineage, released-without-record rejection, native hashes, declared field tokens, residue absence, required capabilities, native XLSX/DOCX/EML structure, print controls, attachment parsing, six-message footprint, mixed-manifest exact-version references, and byte-drift detection.

`python -m unittest discover -s tests -v`

- Result: PASS, 31 tests.

## Residual release work

This slice does not create or claim release records. A control-plane change must bind these exact hashes to blueprint hashes, sanitization evidence, independent Terra/Sol reviews, required proof records, and conductor approval before catalog status changes to `released`.
